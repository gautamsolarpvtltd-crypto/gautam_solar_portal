services:
  - type: web
    name: gautam-solar-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn enhanced_app:app --bind 0.0.0.0:$PORT --chdir /opt/render/project/src
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
